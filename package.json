{
	"name": "quebec-health-data",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev",
		"start": "wrangler dev --env local",
		"test": "vitest",
		"cf-typegen": "wrangler types",
		"create-db:local": "wrangler d1 execute health-db --local --file=./schema.sql",
		"create-db:remote": "wrangler d1 execute health-db --remote --file=./schema.sql",
		"fetch-data": "node src/scripts/fetchWaitlistData.mts",
		"autogen-schema": "node src/scripts/autogenSchema.mts",
		"fetch-regions": "node src/scripts/fetchRegions.mts"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.8.19",
		"@types/node": "^25.0.3",
		"typescript": "^5.5.2",
		"vitest": "~3.2.0",
		"wrangler": "^4.54.0"
	},
	"type": "module",
	"dependencies": {
		"chanfana": "^3.0.0",
		"itty-router": "^5.0.22",
		"shpjs": "^6.2.0",
		"zod": "^4.3.5"
	}
}
